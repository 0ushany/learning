# 自助餐
buffets = ('apple', 'pear', 'meal', 'baccon', 'bear')
for buffet in buffets:
    print(buffet)

# 修改元组元素会报错
# buffets[0] = 250

print("-------------------------------------------------")

# 替换菜品
buffets = ('apple', 'buffer', 'pineapple', 'baccon', 'bear')
for buffet in buffets:
    print(buffet)

# 替换菜品只能够给元组变量直接重新赋值了

