# 立方
# 创建列表，包含前十的整数的立方，输出
values = list(range(1, 11))
cubes = [value**3 for value in values]
for cube in cubes:
    print(cube)


# 简单，生成一个基本列表
# 创建一个立方列表
# 列表的基本值为value**3，也就是立方数
# 列表value的基本值来自于基本列表，1-10
# 输出即可
