# 添加嘉宾

guest = ['oshany', 'shane']
print(guest[0], guest[1] + " just come here to have fun")

print("I got a big table to eat dinner")

guest.insert(0, 'osy')
guest.insert(1, 'newberxy')
guest.append('0shany')

print(guest[0], guest[1], guest[2], guest[3], guest[4] + " have fun now!!")

# 这里使用了insert()方法插入了字符串
# 使用了append往列表末尾插入字符串
